/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}"
    ],
    theme: {
        extend: {
            // Hier können eigene Theme-Erweiterungen hinzugefügt werden.
        }
    },
    plugins: []
};
